server:
  port: 8084
  shutdown: graceful

spring:
  application:
    name: shipping-service
  
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:shipping_db}
    username: ${DB_USER:shipping_user}
    password: ${DB_PASS:shipping_pass}
    hikari:
      maximum-pool-size: 20
      
  jpa:
    hibernate:
      ddl-auto: validate
      
  flyway:
    enabled: true
    baseline-on-migrate: true
    
  cloud:
    stream:
      kafka:
        binder:
          brokers: ${KAFKA_BROKERS:localhost:29092}
          auto-create-topics: false
      bindings:
        # Consumer
        inventoryReserved-in-0:
          destination: inventory.reserved
          group: shipping-service-group
          consumer:
            concurrency: 3
        orderCancelled-in-0:
          destination: order.cancelled
          group: shipping-service-group
          
        # Producer
        shippingEvents-out-0:
          destination: dummy-topic

outbox:
  scheduler:
    enabled: true
    fixed-delay-ms: 1000

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.company.saga: INFO
